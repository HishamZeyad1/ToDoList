<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoList">
        <activity android:name=".ItemTodotaskActivity"></activity>
        <activity android:name=".ItemTodotaskSearchActivity" />
        <activity android:name=".ItemTodolistActivity" />
        <activity android:name=".ListsActivity" />
        <activity android:name=".Login" />
        <activity android:name=".TaskEmptyActivity" />
        <activity android:name=".CheckboxActivity" />
        <activity android:name=".ViewtaskActivity" />
        <activity android:name=".Main1Activity" />
        <activity android:name=".SignUp" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>